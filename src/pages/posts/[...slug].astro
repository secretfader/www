---
import { getCollection } from "astro:content";
import { slugify } from "../../utils/entry";

export async function getStaticPaths() {
  const posts = await getCollection("posts");
  return posts.map(p => ({
    params: { slug: slugify(p.data.title) }
    props: p,
  }))
}



---
